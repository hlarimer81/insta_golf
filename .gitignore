node_modules/
out/
dist/
.aws-sam/
samconfig.toml
.remotion/
*.log
.DS_Store
.env
.env.local
queue.json

# Large media — keep the repo light. Track scripts (JSON), not renders.
public/broll/*
public/audio/*
!public/broll/.gitkeep
!public/audio/.gitkeep
# The music policy needs to travel with the repo, unlike the tracks themselves.
!public/audio/README.md
